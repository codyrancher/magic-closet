{
  "name": "keycloak",
  "description": "Keycloak OIDC/SAML provider.",
  "rancherAuth": {
    "modes": [
      {
        "value": "keycloak",
        "label": "Keycloak (OIDC)"
      },
      {
        "value": "keycloak-saml",
        "label": "Keycloak (SAML)"
      }
    ]
  },
  "port": "KEYCLOAK_PORT",
  "params": [],
  "secrets": [
    "KEYCLOAK_ADMIN_PASSWORD",
    "KEYCLOAK_CLIENT_SECRET"
  ],
  "notes": "OIDC/SAML identity provider at http://keycloak:8080 (host :${KEYCLOAK_PORT}). Realm 'rancher', confidential client 'rancher' / $KEYCLOAK_CLIENT_SECRET, users user1-3. Admin console: admin / $KEYCLOAK_ADMIN_PASSWORD. Enable in Rancher via POST /auth/apply {\"provider\":\"keycloak\"} (or \"keycloak-saml\")."
}
