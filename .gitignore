.env
closet/shared/tools/bin/claude
closet/shared/tools/bin/gh
.state/
workspaces/
# Sidecars created/edited from within a closet — runtime state, shared with
# future compose closets via the bind-mounted repo (git add -f to version one).
custom-sidecars/
