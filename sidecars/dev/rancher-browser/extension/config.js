// Placeholder — overwritten at container start by ext-init, which renders the
// real values from the magic-closet env (RANCHER_*_PASSWORD, APPCO_*).
var MC_CONFIG = {
  creds: [],
  keycloakCreds: [],
  appco: { email: '', token: '' },
};
